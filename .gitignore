# Segredos e Configurações Pessoais
.env

# Dependências do Node
node_modules/

# Build do Next.js e deploy (cache local — não versionar)
.next/
/out/
.vercel

# Logs e arquivos temporários
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Arquivos de diagnóstico e testes temporários
teste_raiz.js
agentes_politicos_rows.csv

# Material de exploração de marca (fica só na máquina, não vai pro deploy)
public/logos/

# Estado local do gerador de resumos (contador diario de tokens da Groq) - nao versionar
coletores/_orcamento_resumos.json
# Backup da versao anterior do gerador, guardado so na maquina - o historico do git ja cobre
coletores/_gerar_resumo_propostas.ANTIGO.js
